services:
  - type: web
    name: genai-support-backend
    env: python
    buildCommand: "pip install -r requirements.txt && python3 -m app.rag.index_builder"
    startCommand: uvicorn app.api.server:app --host 0.0.0.0 --port $PORT
    plan: free
